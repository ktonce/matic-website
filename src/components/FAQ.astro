---
const { items } = Astro.props;
---
<div class="faq">
  {items.map((f) => (
    <details>
      <summary>{f.q}<span class="faq__plus" aria-hidden="true">+</span></summary>
      <p class="faq__answer">{f.a}</p>
    </details>
  ))}
</div>
